{
  "name": "vantix-root",
  "version": "1.1.0",
  "scripts": {
    "build": "if [ -d omnisaas ]; then cd omnisaas && npm run build; else npm run build; fi",
    "start": "if [ -d omnisaas ]; then cd omnisaas && npm run start; else npm run start; fi",
    "install-all": "if [ -d omnisaas ]; then cd omnisaas && npm install; else npm install; fi"
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "framer-motion": "^12.38.0",
    "lucide-react": "^1.8.0",
    "tailwind-merge": "^3.5.0"
  }
}
